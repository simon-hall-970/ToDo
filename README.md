# ToDo App

```
A ToDo app with multiple task and project lists to track daily tasks or more complex projects with multiple subtasks over a longer timeframe.

Built using
node.js, express, react, redux, postgres, superagent, knex, sqlite3
```
